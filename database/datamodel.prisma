type User {
  id: ID! @unique
  firstName: String!
  lastName: String!
  email: String! @unique
  bio: String
  avatar: String
  followingTags: [Tag!]
}

type Tag {
  id: ID! @unique
  title: String! @unique
  description: String!
  followers: [User!]
}
