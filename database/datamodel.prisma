type User {
  id: ID! @unique
  firstName: String!
  lastName: String!
  email: String! @unique
  bio: String
  avatar: String
}

type Tag {
  id: ID! @unique
  title: String! @unique
  description: String!
}
